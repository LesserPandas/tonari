<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="com.tonari.mapper.MyPageMapper">
	<select id ="tjoinpage" resultType="com.tonari.domain.MemberAuthVO">
		select * from member_auth_view where nick = #{nick}
	</select>
	<insert id = "teacherjoin" parameterType="com.tonari.domain.TeacherVO">
		<selectKey keyProperty="bno" order="BEFORE" resultType="int">
			select teacher_seq.nextval from dual
		</selectKey>
		insert into teacher(bno,member_bno,content,image,category,coment,dodate,age,title,tmoney)
		values
		(#{bno},#{member_bno},#{content},#{image},#{category},#{coment},#{dodate},#{age},#{title}, #{tmoney})
	</insert>
	<select id = "getTeacherVO" resultType="com.tonari.domain.Teacherinfo_viewVO">
		select * from TEACHER_INFO_VIEW where member_bno = #{bno}
	</select>
	
	<insert id = "payInsert" parameterType="com.tonari.domain.PayListVO">
		<selectKey keyProperty="bno" order="BEFORE" resultType="int">
			select paylist_seq.nextval from dual
		</selectKey>
		insert into paylist(bno,member_bno,submonth, pay_check)
		values
		(#{bno},#{member_bno},#{submonth},#{pay_check})
	</insert>
	
	<update id="teacherUpdate" parameterType="com.tonari.domain.TeacherVO">
		update teacher set 
		content = #{content},category = #{category},coment = #{coment},dodate = #{dodate},
		age = #{age},title = #{title},tmoney = #{tmoney}
		<if test="image==null">
			,image = #{image}
		</if>
		where bno = #{bno}
	</update>
</mapper>